# TRZ-0013 — Single click opens images and videos in the focus view

_Generated by `kdb`. Do not edit by hand._

Media cards currently keep click-to-select, double-click-to-open (ItemShapeUtil.onClick only opens doors: dir + markdown). Extend the single-click open to image/video kinds so every card is a door, matching Spatial. Gotcha to resolve in design: single-click-open removes the click-to-select affordance for media — selection would need to come from drag-marquee or modifier-click, and resize/move flows must still work (tldraw only fires onClick when the pointer never left the click threshold, so plain drags are safe). Decide whether selection ring/box behavior changes with it.
