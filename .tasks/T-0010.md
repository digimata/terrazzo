# TRZ-0010 — Render Markdown tables in the document writing view

_Generated by `kdb`. Do not edit by hand._

Make GitHub-Flavored Markdown tables render as real tables in the active document writing view rather than remaining pipe-delimited source. Source mode must continue to expose the exact Markdown. In writing mode, render headers, body rows, column alignment, borders, and spacing through CodeMirror decorations or widgets without mounting a separate editor or losing cursor, undo, autosave, conflict, and draft behavior. Define a predictable editing interaction, such as revealing the table source while the cursor is inside the block and rendering it again when focus leaves. Wide tables should scroll horizontally within the centered document column rather than widening the full view. Keep the existing Rust table parsing and canvas-card preview visually consistent. Add tests for alignment markers, empty cells, escaped pipes, inline Markdown inside cells, malformed tables, editing round trips, undo, and source/writing toggles.
