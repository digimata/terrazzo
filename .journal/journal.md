# terrazzo — journal · last 30d

> Rendered view of the global journal (`digimata/.journal/journal.db`), trailing 30-day window. **Do not edit** — regenerated on every entry; full history: `journal render --project terrazzo --since <date>`. `? ·` open question · *decision — …* · *reflection — …*  (+ the why).

## 2026.07.26

13:00 · *decision — terrazzo gate 0 passed; tauri confirmed as shell.* video seek through the asset protocol works (click-to-seek immediate; drag-scrub jank acceptable — detail view owns playback later). 200 shapes at 40-50fps in dev with full-res sources; provisional pass, recheck at m3 on thumbnails. electron fallback in adr-001 is dead. gate closed, m1 starts.
